module.exports = {
  // Default values, based upon community standards.
  printWidth: 80,
  tabWidth: 2,
  useTabs: false,
  semi: true,
  bracketSpacing: true,
  jsxBracketSameLine: false,
  arrowParens: 'avoid',
  proseWrap: 'preserve',

  // Prefer single quotes in all contexts.
  singleQuote: true,
  jsxSingleQuote: true,

  // Prefer trailing commas to minimize noise in diffs.
  trailingComma: 'all',

  // Preserve object-property quoting for APIs with a mixture of conventions.
  quoteProps: 'preserve',

  // Require explicit strings for whitespace in HTML.
  htmlWhitespaceSensitivity: 'ignore',

  // Prefer Unix/Git line-endings.
  endOfLine: 'lf',
};
