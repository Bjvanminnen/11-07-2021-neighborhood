import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root'),
);

/**
Description:
🔎 find your closest neighbor
⚪ draw a square that includes them
♻️ traverse them in the shape of a circle
🔁 repeat

Features:
theme - what are the colors
dot - size of the dot
speed - how fast the dots move
density - where the dots start

*/
